{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build & Run",
            "type": "shell",
            "command": "cmake --build build && ./build/TowerDefense",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}